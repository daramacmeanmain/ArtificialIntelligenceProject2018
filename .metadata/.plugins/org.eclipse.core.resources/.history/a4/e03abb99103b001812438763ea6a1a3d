package ie.gmit.sw.ai;

public class SADecrypt {
	
	public SADecrypt(){
		
	}
	
	public String keyGen(){
		
		for (int i = 0; i < buf.length; ++idx)
            buf[idx] = symbols[random.nextInt(symbols.length)];
        return new String(buf);
		
		return new String(buf);
	}

}
